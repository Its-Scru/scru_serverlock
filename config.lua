config = {
    password = "mypassword", -- Max of 30 characters
    passwordMessage = "Enter server password to continue", -- Message will show above password field
    passwordFailMessage = "Incorrect password", -- Failed message will show after wrong password is entered

    whitelistedUsers = { -- Whitelisted users will bypass the password screen
        "license:XXXXXXXXXXXX",
    },
}